import { ClearTokenInterceptor } from './clear-token.interceptor.js';

describe('ClearTokenInterceptor', () => {
  it('should be defined', () => {
    expect(new ClearTokenInterceptor()).toBeDefined();
  });
});
