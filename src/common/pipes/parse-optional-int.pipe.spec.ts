import { ParseOptionalIntPipe } from './parse-optional-int.pipe.js';

describe('ParseOptionalIntPipe', () => {
  it('should be defined', () => {
    expect(new ParseOptionalIntPipe()).toBeDefined();
  });
});
